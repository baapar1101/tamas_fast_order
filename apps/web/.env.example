# The dev server proxies /api and /uploads to the API (see vite.config.ts),
# so no API URL is needed here. In production, serve dist/ behind the same
# origin as the API (nginx) and nothing changes.
